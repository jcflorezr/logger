log-messages.create-keyspace=CREATE KEYSPACE IF NOT EXISTS ${keyspace-name} WITH replication = { 'class': 'SimpleStrategy', 'replication_factor': '3' };
log-messages.activate-keyspace=USE ${keyspace-name};
log-messages.table-name=log_messages